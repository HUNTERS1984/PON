app_user:
    prefix: /v1
    type: rest
    resource: AppBundle\Controller\UserController
    name_prefix: api_user

app_app_user:
    prefix: /v1
    type: rest
    resource: AppBundle\Controller\AppUserController
    name_prefix: api_app_user

app_store:
    prefix: /v1
    type: rest
    resource: AppBundle\Controller\StoreController
    name_prefix: api_store

app_coupon_type:
    prefix: /v1
    type: rest
    resource: AppBundle\Controller\CouponTypeController
    name_prefix: api_coupon_type

app_coupon:
    prefix: /v1
    type: rest
    resource: AppBundle\Controller\CouponController
    name_prefix: api_coupon

app_post:
    prefix: /v1
    type: rest
    resource: AppBundle\Controller\PostController
    name_prefix: api_post

app_news:
    prefix: /v1
    type: rest
    resource: AppBundle\Controller\NewsController
    name_prefix: api_post
